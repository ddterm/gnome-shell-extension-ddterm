#!/bin/sh

set -e

resolved="$(realpath -- "$0")"
bindir="${resolved%/*}"

case ":${PATH:="$bindir"}:" in
(*:"$bindir":*)
    ;;
(*)
    PATH="$bindir:$PATH";;
esac

exec gjs "${bindir%/*}/ddterm/app/main.js" "$@"
