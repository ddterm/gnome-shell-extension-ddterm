[pytest]
junit_logging = all
log_level = DEBUG
log_format = %(asctime)s,%(msecs)-3d %(levelname)7s %(message)s
log_date_format = %H:%M:%S
markers =
    runtest_cm(func): wrap runtest hooks with context manager
    uses_image(file): test uses specified podman image - added automatically during parametrization
