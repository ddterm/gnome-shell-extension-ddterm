[pytest]
junit_logging = all
markers =
    runtest_cm(func): wrap runtest hooks with context manager
    uses_dockerfile(file): test uses specified podman image - added automatically during parametrization
